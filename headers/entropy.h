#ifndef ENTROPY_H
#define ENTROPY_H

#include <string>
#include <vector>
#include <iostream>
#include <fstream>
#include <cmath>
#include <utility>
using namespace std;
pair<double,vector<double>> bajtEntropija(const string& file);

#endif